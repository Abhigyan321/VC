# VaultChat
VaultChat is a secure, ephemeral messaging platform — no accounts, no identities, no traces. Create password-protected rooms instantly. Every room auto-destructs when participants leave, taking all messages with it. Private by default, temporary by design.
